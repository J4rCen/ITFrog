{
  "name": "itfrog",
  "private": true,
  "scripts": {
    "docker:start": "docker-compose up -d",
    "server:start": "lerna run start --scope=server",
    "client:start": "lerna run dev --scope=client",
    "docker:stop": "docker-compose stop"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
